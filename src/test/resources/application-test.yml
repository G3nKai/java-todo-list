spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/todolist_test_db
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: qwerty12345
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
